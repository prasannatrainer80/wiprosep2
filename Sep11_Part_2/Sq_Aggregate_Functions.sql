-- Aggregate Functions 

-- sum() : used to perform sum() operaton on specific field

select sum(sal) from Emp;

-- avg() : used to perform avg() operation on specific field

select avg(sal) from Emp;

-- max() : used to display max value

select max(sal) from EMp;

-- min() : Used to display min. value

select min(sal) from Emp;

-- count(*) : used to display total no.of records

select count(*) from Emp;
